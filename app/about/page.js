import AboutClient from './AboutClient';

export const metadata = {
  title: 'About — Wisemen Soft',
  description: 'Learn about Wisemen Soft — a software studio built on depth of understanding.',
};

export default function AboutPage() {
  return <AboutClient />;
}
